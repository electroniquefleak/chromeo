// user will see all 12 colors
// user will select color(s) -- a redux dispatch will update the global redux state

const ColorWheel = () => {
    return (
        <>
            This is the ColorWheel
        </>
    )
}

export default ColorWheel;